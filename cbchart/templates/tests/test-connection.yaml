apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cbchart.fullname" . }}-test-connection"
  labels:
    {{- include "cbchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cbchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
